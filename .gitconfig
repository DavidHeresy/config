[user]
	name = David Härer
    email = david@familie-haerer.de
; TODO: Extract alias functions into scripts.
[alias]
	build = "! $GIT/build"
    todo = "! $GIT/todo"
	ignore = "!f() { echo "${1}" >> $(git root)/.gitignore; }; f"
	remove = "!f() { git rm ${1} && git commit -m \"rm '${1}'\"; }; f"
	rename = "!f() { git mv ${1} ${2} && git commit -m \"rename ${1} -> ${2}\"; }; f"
	root = "rev-parse --show-toplevel"
	update = "!f() { git add ${1} && git commit -m ${1}; }; f"

